package com.entelgy.marvel.app.callbacks

/**
 * Interfaz para que un adapter indique cuándo ha llegado al final y que se pueda recargar la información
 */
interface OnBottomReachedListener {

    fun onBottomReached()
}